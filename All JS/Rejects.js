//For js, Arrays, Variable declaration, Variable types, Objects and properties. And Functions.
//Rejects from backend js

function simpleInterest(p,r,t) {
	const si = (p * r * t) / 100;
	console.log(si);
}

function addItem(item){
	todolist.push(item);
}

console.log(todolist[0], todolist[4]);
todolist.push("good");
todolist[0]
todolist[7]

// Number 7
//create a function that6 would take in index value and then display the value of the  array at that index

//Number 8
//create a student record object (studentDetail).
	//the object should create the following 
	//name
	//age
	//address
	//department
		//parentDetails(this should be a sub object inside studentDetail)
		//parent details contain
		//firstName
		//lastName
		//address
	//phoneNumber (this should be an array of two numbers, each number in the array should be 11 digit long)	

//Number 9
//add the property sex and put the value to be an array
	//["male", "female"];

